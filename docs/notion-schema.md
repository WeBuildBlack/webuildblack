# Notion Database Schemas

See `programs/templates/` for JSON schema definitions:
- `cohort-tracker.json` — Member progress tracking per cohort
- `curriculum-template.json` — Curriculum module planning
- `member-directory.json` — Community member profiles
- `event-planner.json` — Event planning and tracking
